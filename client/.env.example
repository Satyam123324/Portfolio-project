# Copy to ".env" for local dev if you need to override the API URL.
# Leave blank in dev — Vite proxies /api to http://localhost:5000 automatically.
# In production, set this to your deployed backend URL, e.g.:
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
